/**
* 题目见：https://leetcode-cn.com/problems/permutation-sequence/
**/

/** 方法一：暴力穷举法（利用next_permutation()函数） **/

class Solution {
public:
    string getPermutation(int n, int k) {
        int arr[n];
        string str;
        for(int i = 0; i < n; i++) arr[i] = i + 1;

        while(--k) next_permutation(arr, arr+n);
        for(int i = 0; i < n; i++) 
            str += to_string(arr[i]);
        return str;
    }
};
